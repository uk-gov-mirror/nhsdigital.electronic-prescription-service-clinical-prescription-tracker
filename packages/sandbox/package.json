{
  "name": "sandbox",
  "version": "1.0.0",
  "description": "Sandbox",
  "main": "sandbox.js",
  "author": "NHS Digital",
  "license": "MIT",
  "scripts": {
    "unit": "POWERTOOLS_DEV=true NODE_OPTIONS=--experimental-vm-modules jest --no-cache --coverage",
    "lint": "eslint  --max-warnings 0 --fix --config ../../eslint.config.mjs .",
    "compile": "tsc --build",
    "test": "npm run compile && npm run unit"
  },
  "dependencies": {
    "@aws-lambda-powertools/commons": "^2.33.0",
    "@aws-lambda-powertools/logger": "^2.32.0",
    "@middy/core": "^7.3.2",
    "@middy/input-output-logger": "^7.1.0",
    "@nhs/fhir-middy-error-handler": "^2.1.82"
  },
  "devDependencies": {
    "@clinicaltracker_common/testing": "^1.0.0"
  }
}
